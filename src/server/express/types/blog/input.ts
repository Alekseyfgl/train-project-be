export interface AddBlogDto {
    name: string;
    description: string;
    websiteUrl: string;
}

export interface UpdateBlogDto {
    name: string;
    description: string;
    websiteUrl: string;
}
